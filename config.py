from typing import Any, Dict

# Name that we gave to the dictionary.
GameData = Dict[str, Any]

# This is the just API that the jeapardy uses so the other modules can know where to send the requests
CUSTOM_API_BASE = "https://litellmproxy.osu-ai.org"
